---
'@tigerdata/storybook-addon-md': minor
---

Publish the package as `@tigerdata/storybook-addon-md`. Update the addon entry in `main.ts` and any imports of the `/runtime`, `/node`, and `/styles.css` exports to the new name. The repository now lives at https://github.com/timescale/storybook-addon-md.
